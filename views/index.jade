extends layout

block content
  h1= title
  form(method="post", enctype="multipart/form-data")
    p
      input(type="text", name="title")
      input(type="file", name="image")
      input(type="submit")

    
