extends layout

block content
  h1= title
  form(method="post", enctype="multipart/form-data", action="createImage")
    p
      input(type="text", name="title")
      input(type="text", name="book")
      input(type="file", name="image")
      input(type="submit")

    
